PakDoc is an utility for generating pakset reports.

Input are pakset sources, scanned automagically.
Output is a static HTML with pictures and a bit of Javascript.

Configuration is currently built into the script.

Reports include ways, stations & extensions, and vehicles.
